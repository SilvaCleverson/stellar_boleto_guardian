{
  "name": "boleto-guardian-api",
  "version": "3.0.0",
  "description": "Boleto Guardian — backend Node.js para deploy em container Docker",
  "main": "server.js",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "@stellar/stellar-sdk": "^13.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
